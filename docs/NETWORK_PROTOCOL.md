# Network Protocol

Сеть отложена до Gate A.

План: listen server, host-authoritative simulation, клиентские команды,
snapshots 10–20 Гц, интерполяция 100–150 мс. Deterministic lockstep не
используется.

Схема сообщений не фиксируется до отдельного сетевого эксперимента.
